<?xml version="1.0" encoding="utf-8"?>
<paths>
    <!-- Generated payslips/documents live under the app-specific external files dir
         (no runtime permission needed) and the cache dir; both are shared/opened
         via FileProvider. -->
    <external-files-path name="external_files" path="." />
    <cache-path name="cache" path="." />
</paths>
