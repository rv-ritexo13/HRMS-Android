<?xml version="1.0" encoding="utf-8"?>
<merge xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    tools:parentTag="android.widget.FrameLayout">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/space_xxl">

        <ImageView
            android:id="@+id/imageStateIcon"
            android:layout_width="@dimen/state_view_icon_size"
            android:layout_height="@dimen/state_view_icon_size"
            android:importantForAccessibility="no"
            android:src="@drawable/ic_error_state"
            app:tint="?attr/colorError" />

        <TextView
            android:id="@+id/textStateTitle"
            style="@style/Widget.HRMS.Text.SectionHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_lg"
            android:gravity="center"
            android:text="@string/employees_error_title" />

        <TextView
            android:id="@+id/textStateMessage"
            style="@style/Widget.HRMS.Text.Caption"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_xs"
            android:gravity="center"
            android:text="@string/employees_error_message" />

        <com.google.android.material.button.MaterialButton
            android:id="@+id/buttonStateAction"
            style="@style/Widget.HRMS.Button.Secondary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/space_lg"
            android:text="@string/action_retry" />
    </LinearLayout>
</merge>
